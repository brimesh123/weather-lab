org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Provide a default namespace for library modules that may be missing one.
# Some older plugin packages do not declare `namespace` in their build.gradle;
# setting this may help AGP configuration in those cases.
android.namespace=com.weatherspinner.app



